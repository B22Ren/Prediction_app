Date  din dataset sunt din 2016 pina in 2/17/2017

Pentru funcționarea corectă și rapidă a aplicației sugerez să lucrați cu un env- virtual enviroment.
Am instalat biblioteca python 3.12 , keras, Tensorflow, Pandas, NumPy, Matplotlib, Statsmodels,
Scikit-learn, PyYAML, bcrypt, PILLOW, Streamlit, os, load_dotenv, time_delta,

Pentru ca aplicației să ruleze scriem în terminal 
(streamlit run app.py) - fara paranteze

Pentru notificari sugerez sa aveti telegram si sa cautari botul
@userinfobot - selectati id si il adaugati in informatii
apoi trebuie sa cautati botul PredictieBot si sa dati start altfel nu se vor trimtie notificari.

Sugerez sa folositi currency_data.csv deoarece el este baza functionarii, 
Unele csv au alte structuri astfel s-ar putea crea unele erori la citirea datelor.


Imaginile se salveaza in profile_images


Dataset from 2016- 02/17/2017

For the correct function of the app u could work with an virtual enviroment env. I instaled libraries from python, keras, tensorflow, pandas, NumPy, Matplotlib, Statsmodels, Scikit-learn, PyYAML, bcrypt, Streamlit, os, Load_dotenv, time_delta

to start the aplication run streamlit run app.py

In order for notifications to work when creating an account u should have telegram instaled , search for @userinfobot select the ID, then search for @predictie_currency_bot and press start.

use the existing currency_data.csv 

The images are saving in the profile_oimages.
